<div align="center">

# Mobile — BrokerOS

**Expo 54 (React Native) Android app for field teams**

[![Expo](https://img.shields.io/badge/Expo-54-4630EB?logo=expo&logoColor=white)](https://expo.dev/)
[![React Native](https://img.shields.io/badge/React%20Native-0.79-61DAFB?logo=react&logoColor=black)](https://reactnative.dev/)
[![Expo Router](https://img.shields.io/badge/Expo%20Router-6-000000)](https://docs.expo.dev/router/introduction/)
[![TypeScript](https://img.shields.io/badge/TypeScript-3178C6?logo=typescript&logoColor=white)](https://www.typescriptlang.org/)

</div>

---

## Overview

The mobile app is built with Expo 54 and Expo Router 6, targeting **Android only** (no iOS configuration). It's designed for field teams — pre-sales executives making cold calls, sales executives doing site visits, sourcing managers meeting brokers, and closing managers creating on-site bookings.

## Directory Structure

```
apps/mobile/
├── app/
│   ├── _layout.tsx               Root layout (font loading, auth redirect)
│   ├── admin.tsx                  Admin entry screen
│   ├── (auth)/                   Public route group
│   │   ├── sign-in.tsx           Login screen
│   │   └── sign-up.tsx           Registration screen
│   └── (dashboard)/
│       ├── _layout.tsx           MAIN SHELL — role detection, tab bar, socket init,
│       │                         push token registration, auto-dialer start
│       ├── index.tsx             Redirects to role-specific home
│       ├── notifications.tsx     Notification center
│       ├── chat/                 Chat screens
│       ├── admin/                Admin screens
│       ├── business-manager/     Business Manager screens
│       ├── channel-partner/      Channel Partner screens
│       ├── closing-manager/      Closing Manager screens
│       ├── director/             Director screens
│       ├── finance/              Finance screens
│       ├── post-sales/           Post-Sales screens
│       ├── pre-sales/            Pre-Sales screens
│       ├── pre-sales-manager/    Pre-Sales Manager screens
│       ├── sales/                Sales screens
│       ├── sales-executive/      Sales Executive screens
│       ├── sales-manager/        Sales Manager screens
│       └── sourcing-manager/     Sourcing Manager screens
│
├── components/                   Shared React Native components
├── constants/                    App-wide constants (colors, sizes)
├── hooks/
│   └── useCallStatus.ts          Syncs auto-dialer call events to backend
├── lib/
│   ├── auth-client.ts            Better Auth expo client + expoClient plugin
│   ├── SocketContext.tsx          Socket.IO context + useSocket() hook
│   └── location-tracking.ts      GPS tracking for site visit verification
│
├── modules/
│   └── auto-dialer/              Custom native Android module (local package)
│       ├── android/              Native Java/Kotlin code
│       ├── src/                  JS bridge
│       └── package.json          Local package (installed via "file:modules/auto-dialer")
│
├── plugins/                      Custom Expo config plugins (e.g. withCmakeShortPath.js)
├── assets/                       Images, fonts, icons
├── google-services.json          Firebase config (gitignored — see setup below)
├── google-services.json.example  Template for the above
└── android/                      Android native project (generated by Expo)
```

---

## Auto-Dialer Module

The auto-dialer is a **custom native Android module** that enables pre-sales executives to make sequential cold calls without manually dialing each number.

- Located at `modules/auto-dialer/` as a local npm package
- Referenced in `package.json` as `"auto-dialer": "file:modules/auto-dialer"`
- Bridged to JS via `hooks/useCallStatus.ts`
- **Do not `npm install` this** — it's a local file dependency

---

## Development

### Prerequisites

- Node.js ≥ 22
- pnpm ≥ 10
- Running backend (the mobile app connects to it via LAN IP)

### 1. Installation

This project is part of a pnpm monorepo. Install dependencies from the root directory:

```bash
cd ../../  # Go to BrokerOS root
pnpm install
```
*(Note: Do not `npm install` the `auto-dialer` module separately; it is linked locally).*

### 2. Environment Setup

You must configure your `apps/mobile/.env` file properly before starting the Expo server.
```bash
cd apps/mobile
cp .env.example .env
```

#### A. Backend API URL (CRITICAL)

Your phone or emulator **cannot reach `localhost`**. You must set `EXPO_PUBLIC_API_URL` to your machine's actual LAN IP.

**Find your IP:**

| OS | Command | Look for |
|---|---|---|
| Windows | `ipconfig \| findstr /i "ipv4"` | Prints only your IPv4 line directly |
| Mac / Linux | `ifconfig \| grep "inet " \| grep -v 127.0.0.1` | The non-loopback inet address |
| Android Emulator (AVD) | — | Use `10.0.2.2` (maps to host's localhost) |

Then update `apps/mobile/.env`:
```env
# Physical device on same Wi-Fi:
EXPO_PUBLIC_API_URL="http://192.168.x.x:3333"

# Android Studio emulator only:
EXPO_PUBLIC_API_URL="http://10.0.2.2:3333"
```

#### B. Google Maps (Required for Site Visits)
The mobile app relies on Google Maps natively for GPS verification of site visits and broker meetings.
1. Go to [Google Cloud Console](https://console.cloud.google.com/apis/credentials).
2. Enable the **Maps SDK for Android**.
3. Generate an API key.
4. Place the key in `apps/mobile/.env`:
   ```env
   EXPO_PUBLIC_GOOGLE_MAPS_API_KEY="your-key"
   ```

### 3. Push Notifications & Expo Dashboard (CRITICAL)
To enable background push notifications and the auto-dialer sync, you must configure Firebase Cloud Messaging (FCM) and link it to your Expo account:

1. **Firebase Setup**:
   - Go to the [Firebase Console](https://console.firebase.google.com/) and create a project.
   - Add an Android App with the package name `com.anonymous.mobile` (from `app.json`).
   - Download the `google-services.json` file and place it in the root of the `apps/mobile/` directory.

2. **Generate Google Service Account Key**:
   - In Firebase, go to Project Settings -> Service Accounts.
   - Click **Generate new private key**. This will download a `.json` file containing your Service Account credentials.

3. **Expo Dashboard Upload**:
   - Run `npx expo login` in your terminal to log into your Expo account.
   - Go to your [Expo Dashboard](https://expo.dev/) in the browser.
   - Select the `mobile` project.
   - Go to **Credentials** -> **Android** -> **Push Credentials**.
   - Click **Add FCM Server Key** (or Service Account Key) and upload the `.json` file you generated in step 2.

*Without this step, Expo cannot send push notifications to Android devices.*

### 4. Running the App (Two Options)

#### Option A: Android Emulator (Android Studio)
1. Open **Android Studio** and start your Virtual Device from the Device Manager.
2. Once booted, run:
   ```bash
   npx expo run:android
   ```

#### Option B: Physical Android Device
1. Enable **Developer Options** and **USB Debugging** on your Android phone.
2. Connect via USB and verify with `adb devices`.
3. Run:
   ```bash
   npx expo run:android
   ```

> **Warning:** Because this app uses custom native code (the Auto-Dialer), **it will NOT work in the standard Expo Go app from the Play Store**. You must use `npx expo run:android` which compiles a custom development app specifically for this project and installs it on your phone/emulator.

---

### Java Home (Windows Troubleshooting)

If your Android build fails with a "Java" or "JDK" error on Windows, you need to point your terminal to the Java version bundled with Android Studio.

Run this command directly in your PowerShell terminal **before** running `npx expo run:android`:

```powershell
$env:JAVA_HOME="C:\Program Files\Android\Android Studio\jbr"
```

---

## Testing (Maestro E2E Flows)

The mobile app includes native E2E test flows defined in `apps/mobile/e2e/`:
- `01-login-and-roles.yaml`: Role switcher, credential validation, and dashboard tab navigation.
- `02-lead-autodialer.yaml`: Lead queue loading, call initiation, and status logging.
- `03-site-visit-verification.yaml`: GPS coordinate acquisition and selfie verification check-ins.
- `04-inventory-unit-booking.yaml`: Unit selection, pricing review, and booking lock request.
- `05-commissions.yaml`: Sourcing manager commission summaries and settlement requests.

### Running Mobile Tests

From the **monorepo root**:

```bash
pnpm test:mobile:e2e
```

> **Note:** Maestro is an external mobile test runner. If not already installed, install Maestro on Windows in PowerShell:
> ```powershell
> Invoke-WebRequest -UseBasicParsing "https://get.maestro.mobile.dev" | Invoke-Expression
> ```
> Ensure an active Android emulator or physical device is connected (`adb devices`) and the app is running (`pnpm dev:mobile`) before launching the test suite.

---

## Building for Distribution

### APK (for sideloading / GitHub Releases)

```bash
cd apps/mobile
eas build --platform android --profile preview
# Download the .apk from the EAS dashboard
# Attach to a GitHub Release for users to download
```

### Internal Distribution (EAS)

The `eas.json` already has a `preview` profile with `"distribution": "internal"`:

```bash
cd apps/mobile
eas build --platform android --profile preview
# Generates a shareable link — anyone with the link can install
```

---

<div align="center">

**[← Back to main README](../README.md)**

</div>
